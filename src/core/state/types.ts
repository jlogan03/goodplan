/**
 * State machine types — thin re-export layer from schemas.
 */
export type { StateEvent, StateError } from "../../schemas/state-events.js";
export { isStateError } from "../../schemas/state-events.js";
