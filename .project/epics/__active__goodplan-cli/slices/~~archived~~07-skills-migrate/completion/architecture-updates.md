# Architecture Updates: skills-migrate

No architecture updates needed. This slice added `skills/` (markdown prompts) and `scripts/install-skills.sh` (shell script) — both are outside the 4-layer CLI architecture described in the epic's architecture files. The CLI architecture (Commands → RPC → State Machine → Filesystem) is unchanged.
